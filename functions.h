#ifndef FUNCTIONS_H
#define FUNCTIONS_H
#include <QString>

QString authorize(std::string login, std::string password);

QString start();
QString finish();
QString next();
QString send_status();

#endif // FUNCTIONS_H
